<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.smkn4bdg.jelita">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Jelita">
        <activity android:name=".daftar.DaftarBerhasilActivity"/>
        <activity android:name=".daftar.DaftarActivity" />
        <activity android:name=".NabungActivity"></activity>
        <activity android:name=".SetorActivity"></activity>
        <activity android:name=".riwayat.RiwayatActivity" />
        <activity android:name=".EditProfile" />
        <activity android:name=".SetorBerhasil" />
        <activity android:name=".WelcomePage" />
        <activity android:name=".Pengepul" />
        <activity android:name=".Profile" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>